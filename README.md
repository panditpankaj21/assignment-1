# INSTALLATION PROCESS
 1. clone the repository
 ```bash
    git clone https://github.com/panditpankaj21/assignment-1.git
 ```
 2. go inside frontend `cd fronted`
 3. do `npm install`
 4. go back and inside backend `cd .. && cd backend`
 5. run `python -m uvicorn main:app --reload`